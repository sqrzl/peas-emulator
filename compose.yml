version: "3.8"

services:
  peas:
    build: .
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      BLOBS_PATH: /app/blobs
      PEAS_ACCESS_TOKEN: developer
      PEAS_ACCESS_SECRET: developer
    volumes:
      - peas-blobs:/app/blobs

volumes:
  peas-blobs:
