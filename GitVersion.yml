mode: ContinuousDelivery
tag-prefix: "[vV]"
next-version: 0.1.0
branches:
  main:
    regex: ^main$
    tag: ""
    increment: Patch
    prevent-increment-of-merged-branch-version: false
ignore:
  sha: []
